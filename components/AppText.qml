import QtQuick
import "../core"

Text {
    id: root
    color: Theme.textPrimary
    font.pixelSize: 14
    // font.family можно задать, если есть стандартный, но оставим по умолчанию
    // Если нужно, сделаем свойство, или просто будем полагаться на систему
}
